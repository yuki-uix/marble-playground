# Asset credits

All board, component and marble geometry is generated by this project's code. No assets from the reference demo are copied. Audio tones are synthesized with the Web Audio API. Interface icons come from lucide-react (ISC license). Fonts use the visitor's system fonts. Third-party dependencies retain their own licenses.
